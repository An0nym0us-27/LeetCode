class Solution {
public:
    vector<int> leftRightDifference(vector<int>& nums) {
        vector<int> answer;

        for(int i = 0; i < nums.size(); i++){
            if((leftSum[i] = nums[0]) || (rightSum[i] = nums.size())){ //should -1 size of nums?
                leftSum[i] == 0;
                rightSum[i] == 0;
            }
            else{
                
            }
        }
    }
};

class Solution:
    def similarPairs(self, words: List[str]) -> int:
        
        myDict= {} #create hashmap

        for strings in words: #add strings to hashmap
            myDict[strings] = len(strings)

        for letters in myDict:
            counter = 0
            
        return counter
